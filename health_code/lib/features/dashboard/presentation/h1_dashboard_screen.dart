import 'package:flutter/material.dart';
class H1DashboardScreen extends StatelessWidget {
  const H1DashboardScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('H-1 Dashboard')));
}
