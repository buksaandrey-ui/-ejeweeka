// lib/core/utils/app_config.dart

class AppConfig {
  /// Toggle this to TRUE before submitting to Apple App Store.
  /// When true, hides all external links, activation code inputs,
  /// and any mention of web payments to comply with Guideline 3.1.1.
  static const bool isAppReviewMode = true; 
}
